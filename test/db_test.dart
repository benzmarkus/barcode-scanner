import 'package:flutter_test/flutter_test.dart';

void main() {
  group('Database Helper', () {
    test("should pass", () {
      expect(1, 1);
    });
    test("should fail", () {
      expect("fail", "fail");
    });
  });
}
